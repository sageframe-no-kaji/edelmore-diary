<script lang="ts">
// URL anchor for the [date] route. Content and state live in the authenticated layout.
import type { PageData } from './$types';
const { data: _ }: { data: PageData } = $props();
</script>
